export interface TimeCapsule {
  title: string;
  content: string;
  closeAt: string;
  imageList: string[];
}

export interface TimeCapsules {
  title: string;
  thumbnailImage: string;
  createdAt: string;
  closeAt: string;
}
